{
  "name": "skinport-relay",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/bobomurod/skinport-relay-test-task#readme",
  "bugs": {
    "url": "https://github.com/bobomurod/skinport-relay-test-task/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/bobomurod/skinport-relay-test-task.git"
  },
  "license": "MIT",
  "author": "@bobomurod on github or @b0b0mur0d can be found on telegram",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test --test-force-exit 'tests/**/*.test.js'",
    "test:unit": "npm run build && node --test --test-force-exit 'tests/unit/**/*.test.js'",
    "test:integration": "npm run build && node --test --test-force-exit 'tests/integration/**/*.test.js'",
    "start": "node dist/server.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "pg": "^8.16.3",
    "uuidv7": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.9.3"
  }
}
